module.exports = {
  webpack(config) {
    // Optimize chunk splitting
    config.optimization.splitChunks = {
      chunks: 'all',
      maxSize: 2500000, // This limits each chunk to 2.5 MB, adjust as needed
    };
    
    return config;
  }
};
